image:
  repository: ghcr.io/connorgallopo/tracearr
  pullPolicy: IfNotPresent
  tag: 1.4.0@sha256:4e70a43844c4bccab7e06d0703551f0cb7398a07a2a636de5366bd7b312c65c0

securityContext:
  container:
    readOnlyRootFilesystem: false

service:
  main:
    ports:
      main:
        protocol: http
        targetPort: 3000
        port: 3000

cnpg:
  main:
    enabled: true
    user: tracearr
    database: tracearr

redis:
  enabled: true
  redisUsername: default

workload:
  main:
    podSpec:
      containers:
        main:
          env:
            PORT: "{{ .Values.service.main.ports.main.port }}"
            JWT_SECRET: "changeme"
            COOKIE_SECRET: "changeme"
            # Optional MaxMind GeoIP license key
            MAXMIND_LICENSE_KEY: ""
            CORS_ORIGIN: "*"
            LOG_LEVEL: info
            # Enable reverse proxy support
            TRUST_PROXY: true
            DATABASE_URL:
              secretKeyRef:
                name: cnpg-main-urls
                key: std
            REDIS_URL:
              secretKeyRef:
                expandObjectName: false
                name: '{{ printf "%s-%s" .Release.Name "rediscreds" }}'
                key: url
