# yaml-language-server: $schema=./values.schema.json
image:
  repository: ghcr.io/atuinsh/atuin
  pullPolicy: IfNotPresent
  tag: 18.16.1@sha256:9d4d79507b51be6d292b218f5cc791d79ca0a72f51ab48f786b091bd6eafb39f

securityContext:
  container:
    runAsUser: 1000
    runAsGroup: 1000

service:
  main:
    ports:
      main:
        protocol: http
        port: 8888

atuin:
  open_registration: false

workload:
  main:
    podSpec:
      containers:
        main:
          args:
            - start
          env:
            ATUIN_HOST: 0.0.0.0
            ATUIN_PORT: "{{ .Values.service.main.ports.main.port }}"
            ATUIN_OPEN_REGISTRATION: "{{ .Values.atuin.open_registration }}"
            ATUIN_CONFIG_DIR: /config
            ATUIN_DB_URI:
              secretKeyRef:
                name: cnpg-main-urls
                key: std
            ATUIN_DB_USERNAME: "{{ .Values.cnpg.main.user }}"
            ATUIN_DB_PASSWORD:
              secretKeyRef:
                name: cnpg-main-user
                key: password

persistence:
  config:
    enabled: true
    mountPath: "/config"

cnpg:
  main:
    enabled: true
    user: atuin
    database: atuin
